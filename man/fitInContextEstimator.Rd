% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/InContextEstimator.R
\name{fitInContextEstimator}
\alias{fitInContextEstimator}
\title{Fit an In-Context Estimator}
\usage{
fitInContextEstimator(trainData, modelSettings, analysisId, analysisPath, ...)
}
\arguments{
\item{trainData}{The training data.}

\item{modelSettings}{A modelSettings object.}

\item{analysisId}{Id of the analysis.}

\item{analysisPath}{Path of the analysis.}

\item{...}{Extra inputs passed to PatientLevelPrediction::fitPlp().}
}
\value{
A plpModel object.
}
\description{
Fits a pretrained binary classifier through PLP's classifier
workflow, registering a separate training context in each partition.
}
\details{
`modelSettings$settings$pythonModule` names an adapter in `inst/python`.
The adapter provides `fit_model(features, targets, seed, ...)`,
`predict_proba(model, features)` returning outcome-1 probabilities,
`get_selected_feature_indices(model)` returning zero-based column indices,
and `save_model(model, path)` / `load_model(path)` using a model directory.
Each adapter preserves its fitted context and preprocessing when saving.
Inputs are non-temporal covariate matrices with binary outcomes.
}
